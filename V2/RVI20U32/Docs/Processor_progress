





primero que todo probar antes quetodo que el programa acrual escriba por consola uart cuando escribo con teclado guardar en un commit luego continuar













proximo crear el bootloader para crgarlo o precargarlo en la rom y definir los limites de rom ram en el programa riscv e zynq 

20/03/2026

* UART1 921000 Baudios Zynq <-> Uart Riscv and UART0 115200 Baudios Zynq <-> PC   
* Programas en C para test Uart, RAM.
* Ram dual port para proxiammente cargar desde el zybq el firmware a la ram con un bootloader en la rom
* bootloader en la RAM +  convertidor uart -> port ram
* Tengo dos salidas tx una de el conversor uart ram para el bootloader y la otra es del modulo comun uart del mmio estan unidas a un mux el cual selecciona una u otra estan conectadas al UART1 del Zynq 


---------------------------------------------------------------------------------------

17/06/2026

* Riscv-test RV32UI + RV32MI are done validated, Riscv core + machine mode(Privileged)   
* rv32mi-p + rv32mi-p-csr  


---------------------------------------------------------------------------------------

01/06/2026

* https://github.com/riscv-software-src/riscv-tests.git    test oficiales
* https://github.com/riscv-software-src/riscv-isa-sim.git  spikes


31/05/2026

* 1. TC_CORE_KEY Validó el pipeline base completo: IF → ID → EX → MEM → WB, ADDI, ADD, SW, LW, writeback, regfile, RAM y forwarding básico ALU→store.

* 2. TC_LOAD_USE_UART_TIMER Validó load-use hazard, stalls/bubble, lw seguido de add, lw seguido de sw, branch dependiente de un load, GPIO MMIO, UART MMIO y lectura básica del timer.

* 3. TC_FULL_PIPELINE_MMIO_WAIT Validó una secuencia más larga combinando forwarding, load-use, branch, stores, loads, GPIO, UART, timer y firma final 0xCAFE0001. El fallo de RAM[10] = 6 vs 5 no parece bug del 
     core; ajusta el assert a >= 5 o == 6.

26/05/2026

* Genere los archivos para cocotb copiando proyecto y quitandole todos los ip xilinx propietarios todavia debo quitar la jerarquia generar un wrapper con modulos simples y copiar todos los v de modulos mas   
  BASIC.v + BASIC_Wrapper.v generar todo de nuevo y reemplazar en el rtl de cocotb


23/05/2026

* correccion de traps, simulacion, mux de excepciones complementados, error de gpio
* Simuacion simple com dos LEDS y 1  suiche de entrada o boton para la ejecucion de activacion de leds con boton.
* pruebas GPIO se determino registrar la lectura tambien.
* test Real funcional de SW - LEDS  MYIR ZU5EV 
* info https://tinytapeout.com/   ASIC

22/05/2026

* MMiO only register periphericals, but not RAM, RAM has 1 latency cycle intrinsec, it´s not necessary to regiter.
* add timeout Periphericals ready to avoid infinite stalls ->  trap access fault = stall > timeout.
* Fmax= 190 Mhz WNS=0.161
* add folder spike to do a comparison in future.
 

19/05/2026

* RV32I privileged mode: traps, CSRs, MRET redirect and pipeline flush.  -march=rv32izicsr


16/05/2026

* Wait-states were added to the RISC-V Core -> MMIO interface to support waiting for memories and peripherals for N cycles.
* Block diagram PDF updated.
* Added gen_bp_torture.py, which generates random ASM code.

This gen_bp_torture.py automatically generates a RISC-V .S file with pseudo-random tests to stress:

>>> python3 gen_bp_torture.py --seed 1234 --tests 50

    - branches (BEQ, BNE)
    - backward loops
    - JAL
    - JALR
    - consecutive branches
    - load-use hazards before branch instructions


03/05/2026

* RV32I is done + add one MMIO + GPIO module and RAM get outside MEM stage to independent data RAM.
* into RISCV_ZYNQ/V2/RVI20U32/rv32i_vscode has a Led program test to see 2 LEDs oscilating each other.


03/02/2026

* Created another Block Design (BD) with ILA and VIO for IF stage verification.

02/23/2026

* Generated blocks with sub-blocks to improve readability.
* Fixed MMIO because address mapping was incorrect: the decoder was using relative addressing with offsets, and RAM was not aligned with that.
* GPIO was also not correctly referenced.
* Simulated ILA in a separate BD to avoid modifying the original RISC-V design.
* Obtained consistent values; further testing required for MMIO GPIO.
* Generated documentation for industrial-level hardware verification.

02/22/2026

* No software application is required at this stage.
* Only need to generate the bitstream, load it via JTAG, and observe using ILA and test points.

02/20/2026

* Generated the hardware_app using the XSA.
* Next step: load the riscv_app using Run As..., then return to Vivado and reload the bitstream to activate the ILA.

02/17/2026

* Generated full bitstream + hardware for a RISC-V RV32I + ZynqMP system in Vivado.
* Loaded the XSA into Vitis.
* Need to generate a .c file in Vitis to enable the ZynqMP clock, which will drive the RISC-V core.

12/15/2025

* Studied videos about RISC-V architecture.

12/13/2025

* Completed CPU connections and added a Zynq block to provide the clock (CPU without branches, only ALU instructions).
* Ran simulation using program_sin_saltos.mem, read by iROM.
* Created a simulation folder and saved tb_cpu_behav_iROM.wcfg.

12/10/2025

* Always review the latest conclusion files.
* Moved CPU project from Pluto directory to a shorter path:
  /media/steckler/xilinx_linux/SDR_custom/SDR/Software
* Interconnected most blocks; some connections still pending.
* Observed that add, sub, mul, div instructions are executed by the ALU block, while store, load, jump, etc., are handled by other blocks.
* This leads to R, I, S instruction formats, involving multiplexers to pack registers and execute operations.

12/02/2025 - 

* Created project CPU_N1 at:
  /media/steckler/xilinx_linux/SDR_custom/SDR/Software/plutosdr-fw/hdl/projects/CPU_N1/CPU_V1
* Processor-only project for simulation; still needs interconnection and simulation.

12/01/2025 - 

* Added a processor to the Block Design (BD):

  * No pipeline
  * No FSM
  * Single-cycle architecture
  * Integrated with PlutoSDR
* Created folder "Processor" in:
  /media/steckler/xilinx_linux/SDR_custom/SDR/Software/plutosdr-fw/hdl/projects/pluto
* Folder contains all processor documentation.

