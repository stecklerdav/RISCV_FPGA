.section .text.start
.globl _start

_start:
    addi x10, x0, 10
    addi x11, x0, 11
    addi x12, x0, 12
    addi x13, x0, 13
    addi x14, x0, 14

loop:
    j loop





Este programa es solo secuencial. La prueba real la haces tú forzando hold=1 desde:

testbench
VIO
señal manual
PASS

Mientras hold=1:

pc no cambia
rom_addr no cambia
pc_exec no cambia
instr_exec no cambia
IF/ID no captura nueva instrucción

Cuando quitas hold, el fetch sigue normalmente.
