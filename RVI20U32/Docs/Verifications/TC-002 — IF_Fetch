
TC-002 — IF / Fetch
Escenario: incremento secuencial de PC con IF1/IF2
.section .text.start
.globl _start

_start:
    addi x1, x0, 1
    addi x2, x0, 2
    addi x3, x0, 3
    addi x4, x0, 4
    addi x5, x0, 5

done:
    jal x0, done

Esperado:

PC incrementa de 4 en 4
no hay stalls
no hay flush
no hay redirect
